# Installation guide

Install from a checkout with `python -m pip install -e .`, or use `python -m pip install .` for a standard installation. Run `bughunter-one --help` and `bughunter-dashboard --help` to verify the console scripts.
