# User guide

Run `bughunter-one https://example.com --output payload.json --reports-dir reports` to create a passive scan payload and reports. View an existing result with `bughunter-dashboard payload.json`. The dashboard reads JSON only and never starts reconnaissance.
